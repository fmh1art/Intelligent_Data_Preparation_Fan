# 图2 v2 生成与质量核查

日期：2026-09-09。执行者：专门绘图 agent `figure_workflow_v2`。

## 依据与交付

- 已阅读 imagegen 技能、共同风格规格和正文“执行反馈、结构化搜索与流程修订”及图2分类映射说明；已通过 view_image 检查旧图。
- 最终文件：`figures/fig02_workflow.png`。
- 实际 PNG：1536×1024 像素，RGB，1,362,274 字节。提示词请求更高原生尺寸，但工具实际仅返回该尺寸；未做插值放大，不声称3072×2048原生输出。
- 使用内置 `image_gen.imagegen`，一次全新生成、一次定向编辑。未使用CLI/API脚本，没有代码代绘、像素改写或叠字。
- 全新生成提示词：`figures/prompts/fig02_workflow_v2.md`。
- 第1轮编辑提示词：`figures/prompts/fig02_workflow_v2_edit1.md`。

## 实际调用与轮次

1. 全新生成只传入完整提示词，不传旧图引用。初稿路径：`/home/fanmeihao/.codex/generated_images/01a08541-80a9-7b31-9d64-a4eb4e1d6868/exec-9a0d868a-14a6-4344-b1ad-cd69c5696f73.png`。主链和回路正确，但出现不符合规格的深色晕染背景、执行输入表擅添10/20、内部对照误用虚线，因此未采纳为最终稿。
2. 将初稿保存到工作区，通过 view_image 查看，内置 imagegen 以 referenced_image_paths 指向该PNG进行定向编辑。编辑仅要求白底与去晕影、清除输入数字10/20、内部对照改为实线，同时锁定全部主要箭头端点、中文标签和布局。
3. 最终工具产物：`/home/fanmeihao/.codex/generated_images/01a08541-80a9-7b31-9d64-a4eb4e1d6868/exec-c0e42255-fe4c-48c5-b314-13db4d4f08ed.png`。原样复制为工作区最终PNG后，再次通过 view_image 逐项复核。

## 学术内容与箭头检查

- 主链依次为“流程规划→程序生成→工具执行→独立验证”，三段实线向右，端点清楚。
- “任务与约束”只下接流程规划；“原始数据”只下接工具执行；“验证依据”只下接独立验证。
- 规划微型图为开始、聚合/过滤候选分支、连接、候选流程。该图是候选操作示意，不表示各方法固定执行这一顺序。
- 程序生成的三行token为groupby(…)、join(…)、select(…)；编号1、2、3表意清楚。
- 执行输入微表保留字段id/value及行标a/b，数值单元格留白，避免未指定操作的数值变换含义。输出示意为a=12、b=8，状态节点S₀→S₁。该微表示意不是实验数据。
- 执行向下“保存”到“中间状态”；状态内容为“表·参数·日志”。
- 独立验证接收外部“结构约束／参考数据／任务判定”。内部参考/结果微表均为a=12、b=8；“对照”连接改成实线，不与语义澄清的虚线约定混用。
- 检查项逐字为“结构一致”“统计合理”“任务满足”。这是一张作者综合机制图，不能据此声称被综述系统都具有完整验证器。
- 验证向下经“通过”到“数据·流程·记录”。
- 失败线从验证器右边缘出发，沿右边、底部、左边独立走廊连续返回，单一终点箭头进入规划器左边缘。未穿过成功产物、状态、澄清模块或其他连线。
- 右边“失败反馈”、底边“定位状态·回退修订”均深琥珀色，白底清晰。该返回线表达修订前序规划，状态恢复依托中间状态记录；未额外引入不存在的直接状态返回箭头。
- “人工澄清”与规划器之间为独立双向虚线，标签“语义歧义”；没有连到程序生成器。
- 底部“执行成功 ≠ 任务正确”与正文一致。

## 视觉核查

最终图在1536像素宽预览下可读。对比旧图，删除长段落和厚重深色标题条；通过短标签、微型DAG、精简程序token、表结构、状态节点与比较连线表达机制。反馈回路与人工澄清路径分离，白底、蓝绿主色和紫/琥珀辅助色明确区分主要流程、状态保存与失败修订。未发现字符缺失、数值错写、重叠箭头或跨卡片连接。保留轻浅色面板，未进一步为追求纯平矢量观感进行代码修改。

限制：工具未遵从更高分辨率请求；最终为LLM生成位图，缩到论文版心后的实际可读性由主agent在编译PDF中继续确认。未修改正文、README、总生成记录，也未提交Git。

主agent集成：候选已按字节原样复制为上述正式PNG，重复候选副本移除；旧版可从重绘前Git提交查看。主agent已独立核对最终完整图、关键数据与图文对应。
