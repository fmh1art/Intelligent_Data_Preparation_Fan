# 学术综述配图生成记录

- 生成日期：2026-09-09。
- 执行者：本次任务专门的学术绘图 agent。
- 实际工具：内置 `image_gen.imagegen`；采用默认内置工具模式，未调用 API/CLI 回退，未使用 Python 或其他代码绘制、改写图像像素。
- 原始文件保留于本机 `$CODEX_HOME/generated_images/01a0847d-831d-7f12-a0fd-699f1b428217/`。最终选择的图像已复制进入仓库；正文不依赖该本机目录。
- 生成图像均为概念综合示意，不是被综述系统的原始架构图。图中微型表格与程序片段仅用于说明数据处理，不代表实测实验结果。

## 图1：任务体系与方法层次

- 首次生成 prompt：[fig01_taxonomy.md](prompts/fig01_taxonomy.md) 中从 `Use case` 到 `Constraints` 的完整英文规格。
- 原始输出：`exec-126a3573-5377-4247-ab5a-90d766bd39f4.png`。
- 最终文件：[fig01_taxonomy.png](fig01_taxonomy.png)，PNG、RGB、1536 × 1024 像素。
- 结果：一次生成后采用。六类任务按两行三列展示，任务卡片之间无顺序箭头；输入、方法支撑条、三类输出及横贯评估维度均已呈现。
- 人工视觉核查：全部指定中文任务与子任务标签可读；输入与输出箭头方向正确；没有裁切或面板重叠；数据表、标签、连接、修复等图标与数据库研究语境相符。
- 工具额外补充了输入面板的短说明，包括结构化/半结构化/非结构化数据、元数据，以及目标、质量、资源时间与领域规则约束；这些说明与图意一致，未引入系统性能主张。

## 图2：执行反馈与验证机制

- 首次生成 prompt：[fig02_workflow.md](prompts/fig02_workflow.md) 中从 `Use case` 到 `Constraints` 的完整英文规格。
- 首次输出：`exec-321a4076-1497-4e3a-b016-1c8336a9733b.png`，未选作最终图。
- 首次核查发现：失败反馈线与中间状态连线相交，验证依据误与失败反馈线相连。因此使用内置图像编辑进行了单次针对性修改。
- 编辑 prompt：[fig02_workflow_edit.md](prompts/fig02_workflow_edit.md)。编辑前通过 `view_image` 查看原图，再将原图路径作为 `referenced_image_paths` 传入内置 imagegen。
- 编辑输出：`exec-7cd43789-42f4-4856-8a98-01748fd28fa7.png`。
- 最终文件：[fig02_workflow.png](fig02_workflow.png)，PNG、RGB、1536 × 1024 像素。
- 最终人工视觉核查：主链为规划→生成→执行→独立验证；原始数据指向执行器；执行器向下保存中间状态；验证依据独立向上输入验证器；通过分支向下输出数据、流程与记录；失败分支沿下方返回规划器；人工澄清使用双向虚线。主要连接线无交叉、无错误汇合。
- 全部指定中文标题和验证依据标签可读；箭头、图标与面板均在画布内。生成工具补充的代码、示例表与检查项用于机制说明；示例人物与数据为图中虚构占位内容。

## 图3：多表准备流程的错误传播、验证与回退

- 初始规格：[fig03_error_propagation.md](prompts/fig03_error_propagation.md)。实际生成采用完整规格，补充 `Use case: scientific-educational`、白边与避免遮挡的排版要求；没有加入新金额、表格行或实验结论。
- 实际工具仍为内置 `image_gen.imagegen`，初次生成后调用一次图像编辑；编辑前通过 `view_image` 查看原图。
- 首次输出：`exec-f9378ae5-9eaa-4b03-ac32-7492a17dd9fd.png`，未选作最终图。初稿的回退箭头误指向错误分支标题，检查连线来自错误结果卡，且预设检查口径未显式标注。
- 完整编辑 prompt：[fig03_error_propagation_edit.md](prompts/fig03_error_propagation_edit.md)。修订为回退到源表状态、错误中间表直接输入独立检查，并添加“依据：预设任务口径”。
- 最终输出：`exec-e11c23b4-bc28-43a0-b9ea-fd95baaa7d46.png`；已复制到 [fig03_error_propagation.png](fig03_error_propagation.png)，PNG、RGB、1536 × 1024 像素。原始两次输出均保留于前述本机生成目录。
- 数值核查：订单源表为 `o1/A/100`、`o2/A/80`，退款源表为 `o1/20`、`o1/10`；错误中间表三行为 `o1/100/20`、`o1/100/10`、`o2/80/0`，逐行相减并汇总为 `80+90+80=250`；先聚合退款为 `o1/30` 后，正确中间表为 `o1/100/30`、`o2/80/0`，净收入为 `(100−30)+(80−0)=150`。全部表格数值、两条结果和正确计算式均在最终图中核对。
- 连线核查：两条执行路径自左向右；错误中间表以独立深蓝箭头指向检查卡；正确结果向上进入检查卡；橙红回退线从检查卡出发，返回左侧连接前源表状态；主要流程线无交叉、无误连。
- 语义核查：顶部说明“按客户汇总净收入；订单金额仅计一次”；错误标记针对本例逐行相减后重复计入订单金额，未将一对多连接本身称为非法。“连接后订单键应唯一”置于本任务的预设口径下，同时使用 `180−30=150` 的总额检查；没有以最终客户键唯一替代过程验证。
- 可读性核查：主要中文标签、任务卡、表格、250/150 结果、预设口径、回退说明和底部框架条清晰，表格无多余行或缺失金额；客户列仅在输入订单表显示，中间表为强调金额重复而省略显示。微型表格中的值是作者构造示例，不是论文实验数据。

## 适用范围与不足

- 三图采用统一的深蓝、青绿与白底，图1和图2辅以低饱和琥珀色，图3用橙红强调错误与回退；使用数据管理图标，没有机器人、机械臂或具身智能场景。
- 三图均是栅格资产，文字和箭头不能像 SVG 一样单独编辑。相同 prompt 的重新生成不保证逐像素复现。
- 当前横向分辨率在 166 mm 排印宽度下约为 235 ppi。主要标签可读；图2的微型表格和补充说明较密，最终投稿若要求 300 dpi 或更高，应按期刊版式重新生成更高分辨率版本并复核。
- 本记录依据图像原尺寸视觉检查，不替代主文档编译后的实际页面布局检查。
