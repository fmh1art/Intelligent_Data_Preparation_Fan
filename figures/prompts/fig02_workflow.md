# 图2：任务感知数据准备的执行反馈与验证机制

插入位置：第4节“任务感知的数据准备流程”末。该图是对现有机制的综合示意，不是AutoPrep、DeepPrep或BAT的原始架构图。

Use case: scientific-educational
Asset type: Full-width conceptual mechanism diagram for a Chinese academic database survey; landscape approximately 3:2, print-ready high resolution.
Primary request: Create a precise academic workflow diagram showing planning, deterministic execution, evidence-based validation, and selective rollback for tabular data preparation. Use restrained navy, teal, muted amber and slate palette, white background, thin crisp outlines, consistent flat data icons and strong readable typography as a professional journal taxonomy figure. Make it rich with miniature tables, code brackets, workflow nodes, and checklist glyphs, without decorative filler.
Composition: At top left a compact intent input with target and document icon labeled “任务与约束”; at top right a compact data input with database and table icons labeled “原始数据”. Main middle row, from left to right, has four large cards with visible directed arrows: “流程规划” (branching operator nodes), “程序生成” (code brackets plus small table), “工具执行” (database engine and miniature output table), “独立验证” (checklist and magnifier). The intent input feeds the planner; the data input feeds the tool executor. Below the executor is a separate small storage card with stacked tables labeled “中间状态”, connected from the executor by a downward arrow. Below the validator is an evidence card labeled “验证依据”, with three clearly legible subordinate labels “结构约束”, “统计检查”, “任务一致性”.
Branch logic: From the validator, a green/teal arrow labeled “通过” goes to a lower-right output bundle with clean table, executable workflow document, and source lineage icons, labeled “数据 · 流程 · 记录”. An amber return path labeled “失败反馈” goes from validator around the outside lower edge of the four middle cards back into the planner, visibly passing beside intermediate-state storage; another small label near this path reads “定位与回退”. A small human reviewer profile icon with speech bubble is placed below the planner, labeled “人工澄清”; its dashed bidirectional link to the planner is labeled “语义歧义”. Add one bottom strip with stopwatch, counter and history icons, exactly “任务正确性 · 人工成本 · 执行开销 · 可复现性”.
Text: Render quoted Chinese labels exactly, large enough for an academic figure at 166 mm width. The image itself must not include a figure number or an external title. No citations or method names. No fabricated numeric benchmarks or accuracy claims.
Constraints: Keep solid arrows for execution/data flow; use dashed line only for human clarification. Ensure return arrow is unmistakably validator-to-planner; never imply that a successful program execution alone proves semantic correctness. Rich but controlled flat icons, no 3D or neon. No robots, mechanical arms, or embodied intelligence motifs. Avoid crossing arrows, tiny illegible text, or clipped labels. All content sits within white margins.
