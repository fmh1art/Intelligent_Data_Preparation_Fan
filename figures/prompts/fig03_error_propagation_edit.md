# 图3：回退目标与检查依据修订 prompt

输入图为首轮生成的 `exec-f9378ae5-9eaa-4b03-ac32-7492a17dd9fd.png`。以下为实际传入内置 imagegen 的完整编辑 prompt。

Use case: scientific-educational. 请对输入的中文学术示例图做一次精确连线修订。保留白底、蓝绿橙红配色、全部表格和数值、标题、任务卡、步骤顺序、两条路径、底部框架条、全部图标和专业风格。不要修改任何金额、订单、客户、计算式，不要增加表格行。
只修订以下三处：
1. 回退弧线的目标必须是左边源表，而不是错误分支标题。删除目前从“独立检查”上方返回错误分支标题的橙红弧线。改为从“独立检查”顶部出发，沿上方留白区域向左弯曲，经任务卡下方横跨到输入订单表上方，最后以清晰箭头指入左侧“输入表A：订单O”顶边，表示恢复源表状态。将“连接前的源表状态”标签移到此箭头目标附近的左上方留白处。保留路径标签“回退并先聚合退款”。不得指向错误中间表、错误路径标题或错误结果。
2. 独立检查应直接接收错误的连接后中间表。删除从红色“A：250（错误）”结果卡向右进入独立检查的绿色短箭头。新增一条深蓝实线箭头，从上方错误路径的“连接后的中间表”右边框上部出发，沿该表右上方的留白水平向右，绕过相减汇总模块和250结果卡，从左侧进入“独立检查”卡；不要穿过这些模块。可采用先水平后折线连接，保留清晰箭头，不能与回退线或其他流程线相交。保持正确路径A：150向独立检查的现有向上箭头。
3. 在“独立检查”标题下方加一行清晰小字“依据：预设任务口径”，确保与下面的两条检查条件不重叠。两条条件仍为“连接后订单键应唯一”和“净收入总和 = 180−30 = 150”。这是本示例按订单粒度汇总的任务口径，不要添加一对多连接非法或禁止的说法。
输出完整修订图，布局留足白边，不裁切、不加额外数字、不改写正确式“(100−30)+(80−0)=150”。
